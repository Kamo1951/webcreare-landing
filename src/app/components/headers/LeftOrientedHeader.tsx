type LeftOrientedHeaderProps = {
  subheader: string;
  header: string;
};

export function LeftOrientedHeader({
  subheader,
  header,
}: LeftOrientedHeaderProps) {
  return (
    <>
      <div></div>
    </>
  );
}
