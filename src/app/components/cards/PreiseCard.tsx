type PreisCardProps = {
  id: string;
  heading: string;
  bodyText: string;
};

export function PreiseCard(id, heading, bodyText): PreisCardProps {
  return (
    <>
      <div></div>
    </>
  );
}
